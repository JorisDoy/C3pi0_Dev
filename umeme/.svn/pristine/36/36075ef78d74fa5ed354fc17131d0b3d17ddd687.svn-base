package com.indra.iopen.repositories.model;

import org.springframework.data.repository.CrudRepository;

import com.indra.iopen.model.SumExp;
import com.indra.iopen.model.SumExpPK;

/**
 * The persistent interface for the SUM_EXP database table.
 *
 */
public interface SumExpRepository extends CrudRepository<SumExp, SumExpPK> {
	SumExp findSumExpByIdNumExp(String numExp);
}